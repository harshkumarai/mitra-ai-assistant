"""
memory/__init__.py
------------------
Marks the `memory` directory as a Python package.
The memory layer is split into two concerns:

  context.py  — short-term in-memory conversation turns (this session only)
  history.py  — long-term persistent log of all commands (across sessions)
"""
